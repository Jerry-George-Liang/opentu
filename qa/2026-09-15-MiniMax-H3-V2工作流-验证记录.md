# MiniMax-H3 V2 工作流验证记录

## 当前范围（2026-09-21 本地视频引用增量）

- 新 MiniMax-H3 任务固定 V2，参数面板不显示接口版本；V1 兼容常量和版本解析保留，不作为本轮验收路径。
- 提示词增强默认关闭。开启后先增强，成功后自动生成视频，无独立审阅弹窗或复制到画板步骤。
- 主输入栏按界面语言在 Context IR 文本末尾附加语言指令，中文为“请用中文输出”；不转换结果、不增加 language 请求字段。语言遵循情况由上游模型决定。
- 已完成、具有有效远端 ID 的 MiniMax-H3 768P 视频可升 2K；2K、其他分辨率、未完成、缺少或空白 ID 在 UI 和业务层拒绝。
- 历史 1280x720 / 720x1280 参数按 H3 原请求的默认 768P 兼容。
- 不限制 Key 分组，不自动切换供应商。增强使用当前供应商，重制保留源任务路由。
- 任务队列进度与终态回写保留 remoteId、invocationRoute，避免刚生成的视频丢失升 2K 所需信息；不恢复已丢失的历史 ID。
- 主输入栏的视频模式可选择或拖入本地视频，素材库仅显示图片和视频；H3 最多接受 3 个视频引用，其他不支持模型继续走既有能力校验。
- 本地 `/asset-library/` MP4/MOV 在请求时从缓存转换为 Data URL，公网 HTTP(S) 视频尝试读取并转换，读取失败或类型不匹配时保留原地址。工作流不持久化 Base64。
- 考虑 Base64 膨胀，本地 H3 参考视频合计限制为 47 MiB，完整 JSON 请求体限制为 64 MiB；更大视频需使用公网 URL。

## 接口

- 普通生成：POST /v2/video_generation。
- 提示词增强：POST /v2/h3_context_ir，model=MiniMax-H3，发送 content、duration、ratio，不发送 resolution；完成后读取 task.content.prompt。
- 重制：POST /v2/video_regeneration，仅发送 model=MiniMax-H3、source_task_id、resolution=2K。
- 统一查询：GET /v2/query/video_generation/{task_id}。
- 参考视频：content[].video_url，role=reference_video；支持 MP4/MOV，最多 3 个。缓存缺失、格式错误、非公网地址、本地视频合计超过 47 MiB 或完整请求体超过 64 MiB 时，不发送供应商请求。
- HTTP 错误、非零 base_resp.status_code、任务失败/取消、缺失任务 ID 或增强结果均终止；增强轮询有次数上限，不在失败后提交视频。
- 源任务是否归属当前账号、是否有效及是否超过保留期限由服务端校验；不能用本地资格检查证明上游可接受。

## 合并与修正

- 分支：dev/tuzi-account-workspace；目标：ljquan/opentu 的 develop。
- 本轮重新 fetch 后 upstream/develop 为 b730d011，已包含在合并提交 8cc42685 中。
- 检查 7 个先前冲突文件，固定 V2 路由和保留其他模型调用逻辑；没有未合并条目。
- 修正合并带入的 video-api-service.test.ts 旧 V1 路由断言。
- 为任务队列测试中的 window stub 补充 location.origin，解决上游依赖读取 origin 时的测试初始化失败，不改生产逻辑。
- 移除不再使用的 3 个未跟踪增强弹窗文件，可从保留的 stash 恢复。
- 原有调试页、手册返回按钮、本地清理和媒体失效链接清理改动留在工作区，不进入本次暂存范围。
- QA 使用本记录，DOC 更新现有 OpenSpec，不另建文档体系。

## 实际验证

环境：macOS，本地已有依赖。测试使用 mock，不使用真实 Key。

### 2026-09-19 V2 工作流合并验证

以下命令退出码 0：13 个文件、185 项全部通过。

```bash
pnpm exec vitest run \
  packages/drawnix/src/constants/__tests__/model-config.test.ts \
  packages/drawnix/src/services/video-api-service.test.ts \
  packages/drawnix/src/services/minimax-h3-video-workflow.test.ts \
  packages/drawnix/src/services/minimax-h3-regeneration-service.test.ts \
  packages/drawnix/src/services/__tests__/media-api-routing.test.ts \
  packages/drawnix/src/services/media-executor/fallback-utils.test.ts \
  packages/drawnix/src/services/__tests__/task-queue-service-image-retry.test.ts \
  packages/drawnix/src/services/__tests__/task-queue-service.test.ts \
  packages/drawnix/src/components/ai-input-bar/ModelDropdown.test.tsx \
  packages/drawnix/src/utils/__tests__/runtime-model-discovery.test.ts \
  packages/drawnix/src/utils/__tests__/model-grouping.test.ts \
  packages/drawnix/src/utils/__tests__/provider-base-url.test.ts \
  packages/drawnix/src/mcp/tools/generation-context-queue.test.ts
```

覆盖固定 V2 和历史 v1 参数、增强关闭与成功串行生成、中英文指令、增强失败/超时、分组请求透传、重制资格与供应商、远端 ID 进度/成功/失败回写，以及模型发现和其他视频模型请求回归。

其他检查：

- `pnpm exec tsc -p packages/drawnix/tsconfig.lib.json --noEmit`：通过。
- `NX_DAEMON=false pnpm nx run web:build --skip-nx-cache`：通过，包含 web:typecheck、应用和 Service Worker 构建；保留 Sass、chunk、动态导入等警告。该构建基于当前工作区，含未暂存的既有本地改动。
- 针对暂存区全部 TS/TSX 文件的 ESLint：3 errors、136 warnings。错误位于 DialogTaskList、TaskItem、TaskQueuePanel 的既有 @aitu/utils 静态导入，规则为 @nx/enforce-module-boundaries。将 upstream/develop 的三个原文件通过 ESLint stdin 在同一环境检查，均复现相同规则错误；未屏蔽规则或为此重构业务代码。
- 上述检查之外的较小定向 ESLint 子集曾通过，但不能据此声称完整暂存范围 lint 通过。
- `git diff --check` 和 `git diff --cached --check`：通过。
- 首次直接执行 Vite 的构建命令因入口路径不正确失败，改用项目标准 Nx 构建后通过，未更改构建配置。
- 首轮测试 183/185，通过修正旧断言和 window stub 后最终 185/185。
- 全量测试、全量 lint 和循环依赖检查本轮未执行。OpenSpec CLI 未安装，strict 校验未执行。
- 测试有 Node localStorage/crypto、Browserslist、NPM_TOKEN 缺失等环境警告，失败路径会输出预期错误日志，不影响最终测试退出码。

### 2026-09-21 本地视频引用增量验证

以下命令退出码 0：4 个文件、112 项全部通过。

```bash
pnpm exec vitest run \
  packages/drawnix/src/services/video-api-service.test.ts \
  packages/drawnix/src/services/minimax-h3-video-workflow.test.ts \
  packages/drawnix/src/components/ai-input-bar/canvas-association-resolver.test.ts \
  packages/drawnix/src/components/ai-input-bar/__tests__/workflow-converter.test.ts
```

覆盖本地 MP4/MOV 缓存转 Data URL、公网 URL 原样透传、缓存缺失与格式拒绝、最多 3 个视频、47 MiB 本地合计和 64 MiB 最终请求体边界、H3 别名工作流透传，以及 `VideoAPIService` 最终发送体。

定向 ESLint 覆盖本次 13 个 TS/TSX 文件：0 errors、51 warnings。修复了本次引入的 `@aitu/utils` 静态导入错误和视频 MIME 非空断言；剩余警告均位于既有大文件的原有代码，本轮未为清警告扩大重构。

- `pnpm nx run drawnix:typecheck`：通过。
- `NX_DAEMON=false pnpm nx run web:build --skip-nx-cache`：通过，包含 `web:typecheck`、应用和 Service Worker 构建；保留 Sass、动态/静态导入和大 chunk 等既有警告。
- `git diff --check`：通过。
- 测试仍输出 Node localStorage/crypto、IndexedDB writer 超时、Browserslist 和 NPM_TOKEN 缺失等环境提示，不影响最终退出码。

## 未执行项与验收边界

未执行浏览器交互、视觉测试、完整 AIInputBar 页面端到端测试或真实计费 Context IR/生成/重制请求。没有真实上传 47 MiB 视频验证浏览器内存峰值；自动测试不能证明任何分组已在真实上游开通。未新增依赖、配置或存储迁移。

人工验收：

1. 参数面板隐藏接口版本，增强默认关闭。
2. 关闭增强只提交普通 V2；开启后 Context IR 成功才自动提交视频，不出现旧弹窗，不重复增强。
3. 增强失败显示供应商错误，不产生视频请求；中英文要求跟随界面语言。
4. 已完成的 768P H3 任务可升 2K，2K 或缺失源 ID 时不可用且说明原因。
5. 重制正常轮询、展示、保存，其他视频模型不改变请求逻辑。
6. 视频模式可上传、拖入或从素材库选择本地 MP4/MOV；H3 请求包含 reference_video，超过 3 个或本地合计超过 47 MiB 时显示明确错误且不发请求。
7. 公网视频尝试转为 Data URL，无法读取时保留 URL；本地虚拟地址仅在提交时转为 Data URL；不支持视频引用的模型仍阻止提交。

## 提交状态与注意事项

### 2026-09-22 范围收敛验证

仅保留 MiniMax 视频输入相关未提交改动，包括上传、素材库筛选、画布引用、请求转换及对应测试。重新获取按钮、结果缓存策略、失效链接清理、本地清理与返回入口增量均已撤回到 HEAD；已提交历史功能不变。撤回前文件已归档到仓库外，可恢复。

重跑上述四个视频输入测试文件：115/115 通过；`pnpm nx run drawnix:typecheck` 与 `git diff --check` 通过。测试仍有 localStorage/crypto、IndexedDB writer、Browserslist 和 NPM_TOKEN 环境提示，退出码为 0。本次未重跑完整构建、全量 lint、浏览器交互和真实计费请求，不宣称画布视频已获真实上游验收。

本记录对应本次 PR 范围。提交者应知悉上述上游 lint 基线错误；若再次同步上游，应重跑受影响检查。回滚仅撤回本功能代码，不删除已有任务或用户数据。2026-09-21 本地视频引用增量仍在本地工作区，未提交、未推送、未部署。
