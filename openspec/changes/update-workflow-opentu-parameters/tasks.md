# Tasks: 工作流 OpenTu 参数适配

## 0. Proposal

- [x] 0.1 核对截图对应组件、模型目录、节点参数和原生生成桥接。
- [x] 0.2 核对宿主参数表及 MiniMax-H3、Seedance、图片、文字和音频的关键差异。
- [x] 0.3 写入提案、设计和可执行验收场景。
- [x] 0.4 OpenSpec strict 校验。
- [x] 0.5 用户批准本次参数与控件扩展方案（用户指令：开始）。

## 1. Shared Model Contract

- [x] 1.1 从完整内置目录、启用渠道发现模型、手工绑定和默认路由建立逐模型覆盖清单，记录参数、素材、适配器、最终请求消费者、结果类型和证据，不限于截图或默认模型。
- [x] 1.2 扩展无凭据模型快照、参数类型、输入能力与运行时校验。
- [x] 1.3 按渠道与模型刷新原生元数据，保留脚本和节点有效选择。

## 2. Video First

- [x] 2.1 视频控件从模型描述读取清晰度、尺寸、比例、时长、模式和专属字段。
- [x] 2.2 对齐 MiniMax-H3 的 768P/2K、4-15 秒、ratio 和 api_version。
- [x] 2.3 对齐 Seedance、Kling、Veo、Sora 及其他现有视频适配器的参数和输入角色。
- [x] 2.4 参数摘要、首尾帧/参考素材控件与提交数据使用同一解析结果。

## 3. Other Capabilities

图片、文字和音频与视频均属于本次必做范围，本章节不是可选后续事项。

- [x] 3.1 图片面板区分尺寸、分辨率、画质、数量和输出格式等模型能力。
- [x] 3.2 文字面板与桥接传递系统提示词、上下文及执行器支持的生成参数。
- [x] 3.3 TTS 和 Suno 按各自能力提供参数，并补齐歌词/多片段音频结果回填。宿主没有原生 TTS 引擎，保留独立渠道/脚本 TTS；不虚构原生能力。

## 4. Persistence And Execution

- [x] 4.1 各入口使用按渠道、模型、能力隔离的节点参数；覆盖切换、刷新和重试。
- [x] 4.2 宿主校验当前目录、白名单、字段类型、合法值和素材约束。
- [x] 4.3 参数逐项到达实际适配器消费者，必要时最小扩展宿主接口并加回归。
- [x] 4.4 覆盖取消、错误结果、来源节点归属和脚本优先级。

## 5. Acceptance

- [x] 5.1 运行契约、状态、桥接与实际请求构造的定向测试。
- [x] 5.2 完成桌面/手机、浅深主题浏览器验证并记录截图。
- [x] 5.3 验证普通画布和自定义脚本不受影响。
- [x] 5.4 更新工作流 CHANGELOG 与 pending-test，将实际完成项目从 todo 移出。
- [x] 5.5 分别列明本地验证、供应商未验证项与不支持的能力，逐项核对覆盖清单。
- [x] 5.6 完整模型集合与适配覆盖清单对账，确保没有漏项；新增模型/参数缺少映射时覆盖检查必须失败。
- [x] 5.7 确认宿主已支持的能力不存在工作流未适配项，不以禁用控件、隐藏模型或代表模型通过替代全面完成。

## Current Evidence

2026-09-20 已完成实现与本地验收。逐模型参数/消费者清单位于 `output/playwright/native-model-coverage.md`，原始无凭据快照为同目录 `native-model-snapshot.json`，浏览器全目录结果为 `native-ui-matrix.json`。

- 全部 101 个内置模型：图片 34、视频 36、文字 30、音频 1。包括选择器原先隐藏的 Sora/文字定义。新鲜浏览器配置的两个 legacy 分组共 202 条记录，198 条 configured，4 条 upstream-unavailable（两个分组各自的 Kling O1 / O1 Edit）。此数量不代表用户浏览器全部已配置供应商。
- 宿主/共享契约及普通画布回归共 248 项通过（联合 247 项通过后，新增移除原生渠道保留脚本用例，受影响文件 9/9 重验通过）；前端 49 项、MJ 消费者 4 项通过。全目录遍历所有 101 个定义，全部 71 个非文字模型逐字段送入宿主适配器捕获；文字实际请求覆盖 system/user/assistant 和生成参数。
- 实际浏览器逐条渲染 202 条参数描述，控件数量与默认值校验通过，无 pageerror。生产嵌入页完成原生元数据握手、模型选择、参数编辑/刷新和返回普通画布验证；画布参数弹层在 1440x900 与 390x844 的浅深主题下均未超出视口。截图 `native-canvas-{desktop,mobile}-{light,dark}.png`、`native-embedded-workbench.png`。
- `node scripts/build-workflow.mjs` 成功，新资源已写入 `apps/web/public/workflow-app/`。前端 tsc 仍有既存 `model-script-editor.tsx:58` Modal styles.content 类型错误；宿主 tsc 仍有既存插件 SDK 模块解析/隐式 any 错误。不能称全仓类型检查通过。
- 图片/视频结果返回媒体 URL，文字返回 text，Suno 返回 audio 全部片段或 lyrics 文本节点。取消/来源与 origin 校验有契约测试；画布跨节点取消回填保护经过控制流审查，未执行全部供应商异步竞态的浏览器端到端测试。
- 未发起真实供应商生成，未验证计费、额度、供应商远端取消或全部线上模型开通状态。宿主无原生 TTS 引擎、无 Kling O1 默认适配器、无 Suno 新素材上传引擎；Seedance 参考视频只接受公网 HTTP(S)。绑定不相容的画质字段按实际消费者报告限制，不将宿主缺失能力算为已接通。
